{
  "builds": [
    {
      "src": "app.mjs",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "pnpm clean && tsup"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.mjs"
    }
  ]
}
