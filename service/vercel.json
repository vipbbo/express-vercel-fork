{
  "installCommand": "pnpm install",
  "outputDirectory": "build",
  "buildCommand": "pnpm clean && tsup",
  "devCommand": "node ./build/app.mjs",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "build/app.mjs"
    }
  ]
}
