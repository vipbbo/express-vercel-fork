{
  "builds": [
    {
      "src": "build/app.mjs",
      "use": "@vercel/node",
      "config": { "buildCommand": "pnpm clean && tsup" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "build/app.mjs"
    }
  ]
}
