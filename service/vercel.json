{
  "version": 2,
  "builds": [
    {
      "src": "tsconfig.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "command": "pnpm clean && tsup",
    "synchronousHooks": false
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "build/app.mjs"
    }
  ],
  "env": {
    "NODE_ENV": "prod"
  }
}
